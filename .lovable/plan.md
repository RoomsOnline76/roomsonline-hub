
The user wants two things:
1. **Backfill coordinates** for all properties that have an address but no lat/lng — using Google Maps geocoding (and parsing Google Maps URLs where present).
2. **Enforce coordinates as mandatory** going forward.
3. **Verify Jongensfontein** properties specifically all end up with coordinates (needed for Step 3 RU push).

Let me check current state.
