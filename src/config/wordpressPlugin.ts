/**
 * Single source of truth for the ROL'OS WordPress plugin version.
 *
 * Keep this in sync with the default in
 * `supabase/functions/wordpress-plugin-update/index.ts`.
 *
 * 2.1.0 — white-label support + [rolos_portfolio_booking] shortcode.
 */
export const ROLOS_WP_PLUGIN_VERSION = "2.1.0";
